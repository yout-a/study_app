# === Ruby / Bundler
/.bundle
/vendor/bundle

# === Rails
/log/*
!/log/.keep
/tmp/*
!/tmp/.keep
/storage/*
!/storage/.keep
/public/assets
/public/packs
/public/system
/app/assets/builds/*
!/app/assets/builds/.keep

# Active Storage (アップロード一時/保存先)
# 既にS3等ならこのままでOK。ローカル保存も成果物は追わない。

# === Node / JS ツール
/node_modules
/.pnpm-store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.esbuild
/.vite
/.turbo

# === Tests & Coverage
/coverage
/spec/examples.txt

# === Databases (SQLite使う場合のみ)
/db/*.sqlite3
/db/*.sqlite3-journal
# MySQL/MariaDB はDBファイルが無いので上記は影響なし

# === Credentials / Keys / Secrets
/config/master.key
/config/credentials/*.key
# .enc はコミット対象（暗号化済み）。key は絶対にコミットしない

# === Env files
.env
.env.*
!.env.example

# === Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# === Byebug / History
.byebug_history
