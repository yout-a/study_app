<!DOCTYPE html>
<html>
  <head>
    <title>Study_App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- CSS 読み込み -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "base", "data-turbo-track": "reload" %>

    <!-- JS 読み込み -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% flash.each do |type, message| %>
      <% text =
          if message.is_a?(Symbol)
            I18n.t("devise.sessions.#{message}", default: I18n.t(message, default: message.to_s))
          else
            message
          end %>
      <div style="padding:10px 14px; margin:6px 0; border-radius:4px;
                  <%= type == "notice" ? "background:#ecf;" : "background:#fee2e2;" %>">
        <%= text %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
